#include <stdio.h>
#include <ctype.h>

int main()
{
    char string[50];
    fgets(string, sizeof(string), stdin);
    for(int i = 0; string[i] != '\n'; i++)
    {
        for(int j = 0; j < )
    }

}